import React from 'react';

const Loading = () => {
    return (
        <div className={"absolute inset-0 bg-cyan-100 grid place-items-center"}>
            Loading ...
        </div>
    );
};

export default Loading;